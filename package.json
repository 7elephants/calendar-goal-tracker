{
  "name": "calendar-goal-tracker",
  "version": "1.5.0",
  "private": true,
  "description": "Google Calendar add-on (Apps Script) for tracking daily goals/habits as all-day events.",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "login": "clasp login",
    "create": "clasp create --type standalone --title \"Goals & Habits Tracker\" --rootDir ./src",
    "push": "clasp push",
    "open": "clasp open",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "jest": "^29.7.0"
  }
}
